    import XCTest
    @testable import UIProgressTextView

    final class UIProgressTextViewTests: XCTestCase {
        func testExample() {
            // This is an example of a functional test case.
            // Use XCTAssert and related functions to verify your tests produce the correct
            // results.
            XCTAssertEqual(UIProgressTextView().text, "Hello, World!")
        }
    }
